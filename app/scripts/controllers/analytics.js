'use strict';
angular.module('aifxApp').controller('analyticsController', function($scope, $ionicSideMenuDelegate) {
    $scope.$watch(['selected.cross1', 'selected.cross2'], function(n, o) {
        debugger;
    });
});